import { MydirectivesDirective } from './mydirectives.directive';

describe('MydirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new MydirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
